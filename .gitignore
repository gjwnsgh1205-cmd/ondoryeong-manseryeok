# 개발 중 만들어지는 것들 — 저장소에 들어갈 이유가 없다
.gstack/
.context/
dist/
dist.tmp/
_docs.tmp/
shots/
__pycache__/
*.pyc

# 배포본은 docs/ 하나만 둔다. dist/ 는 단일파일 번들용 임시 산출물이다.

# 워크플로에 원고를 넘기려고 잠깐 만드는 파일들.
# 결과는 tools/*_source.json 에 남고 이건 중간 산물이라 저장소에 둘 이유가 없다.
tools/_*.json
